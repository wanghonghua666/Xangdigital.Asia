rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 允许图片上传和读取（30天有效期，开发阶段）
    match /{allPaths=**} {
      allow read, write: if request.time < timestamp.date(2025, 7, 31);
    }
  }
}
 